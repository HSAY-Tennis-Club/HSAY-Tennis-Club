# HSAY 网站 + 微信小程序架构方案

## 1. 核心原则

网页与微信小程序共享业务，不强行共享界面。

- 共享：球员、赛事、盘分、排名、H2H、指标计算、权限规则、API 类型、设计 token。
- 分离：Web 页面组件与微信小程序页面组件。Web 适合 SEO、分享与大屏数据，小程序适合微信登录、订场入口、扫码和消息订阅。
- 私密信息只由服务端根据身份和角色返回。前端“隐藏”或模糊处理不构成权限保护。

## 2. 推荐单仓结构

```text
apps/
  web/                 # Next.js / 当前 Web 产品
  miniprogram/         # Taro 或原生微信小程序
  admin/               # 比赛录入与会员管理后台
packages/
  domain/              # 排名、H2H、指标计算，纯 TypeScript
  api-client/          # OpenAPI 生成的 Web/小程序客户端
  contracts/           # Zod schema、DTO、权限可见性定义
  design-tokens/       # 颜色、字号、间距、圆角
services/
  api/                  # 身份、权限、比赛、排名、指标 API
```

第一阶段不必立刻拆成以上全部目录；当小程序启动时再迁移成 monorepo，可以避免过早复杂化。

## 3. 部署建议（面向上海用户）

正式产品建议将 API、数据库和图片部署在中国大陆可稳定访问的腾讯云或微信云开发环境；Web 同域或同区域部署。当前 Sites 版本用于快速预览和验证品牌/交互，不作为中国大陆生产基础设施的最终选择。

建议组合：

- Web：Next.js，部署到腾讯云 EdgeOne Pages 或容器服务。
- 小程序：Taro + React；若团队更看重微信原生能力，则选原生小程序。
- API：NestJS/Fastify 或腾讯云函数，提供版本化 REST API。
- 数据库：TencentDB PostgreSQL。早期也可使用微信云数据库，但关系型比赛与统计查询更适合 PostgreSQL。
- 图片：COS 对象存储 + CDN。
- 异步任务：排名重算、赛后指标计算、消息提醒放入队列/定时任务。

## 4. 身份与角色

同一个 `user` 可以绑定多个登录身份：微信 `unionid/openid`、手机号、Web OAuth。业务数据只依赖内部 `user_id`，不直接依赖某个登录渠道。

| 角色 | 可见范围 | 主要操作 |
|---|---|---|
| 游客 | 公开俱乐部资料、公开赛果、排名、基础球员卡、基础 H2H | 浏览与分享 |
| 会员 | 游客内容 + 自己的技术指标、训练记录、私密备注 | 更新本人资料、报名活动 |
| 教练 | 获授权球员的指标与教练备注 | 写训练建议、确认技术标签 |
| 赛事管理员 | 会员名单、比赛与赛程 | 录入/确认赛果、处理退赛 |
| 超级管理员 | 全部业务配置和审计记录 | 权限管理、数据纠错 |

登录只证明“你是谁”；会员表与角色表决定“你能看什么”。所有私密查询和写操作都必须在 API 层再次鉴权。

## 5. 公开与私密字段

| 对象 | 游客可见 | 登录后按权限可见 |
|---|---|---|
| 球员 | 昵称、头像、NTRP、打法、排名、公开胜率 | 姓名、联系方式、训练目标、伤病/可约时间（本人或授权角色） |
| 比赛 | 日期、场地、对阵、盘分、赛事阶段 | 赛后备注、视频、详细技术统计、申诉记录 |
| H2H | 交手次数、胜负、最近赛果 | 场地/球速拆分、战术倾向、关键分与球路洞察 |
| 指标 | 可选的公开摘要 | 六维雷达、趋势、逐场明细、教练私密备注 |

每个球员应有独立的隐私设置；默认采用最小公开原则。

## 6. 核心数据模型

- `users`：账号主体、状态、最后登录时间。
- `identities`：微信/手机号/Web 登录身份与内部用户关联。
- `members`：会员编号、昵称、NTRP、公开简介、隐私设置。
- `roles`、`user_roles`：角色与作用域。
- `tournaments`、`events`：赛事、组别、赛制。
- `matches`：状态、日期、场地、赛制、胜者、录入人、确认状态。
- `match_players`：单打/双打参赛者与队伍。
- `match_sets`：每盘比分、抢十、退赛等结果。
- `ratings`：每期战力分、排名、变动、算法版本。
- `performance_metrics`：球员逐场原始指标与来源。
- `metric_snapshots`：六维指标快照、时间窗口和算法版本。
- `coach_notes`：作者、球员、正文、可见范围。
- `media_assets`：比赛视频/图片元数据与访问级别。
- `audit_logs`：私密数据访问和关键变更记录。

关键约束：比赛结果必须有 `draft → pending_confirmation → confirmed → void` 状态流；排名只读取 `confirmed` 比赛；所有统计保留 `algorithm_version`，以后调整算法仍可追溯。

## 7. API 边界

公开接口与会员接口分开，降低误泄露风险：

```text
GET  /v1/public/club
GET  /v1/public/rankings?season=&level=
GET  /v1/public/matches?cursor=
GET  /v1/public/players/:slug
GET  /v1/public/h2h?left=&right=

GET  /v1/me
GET  /v1/me/metrics?window=8_matches
GET  /v1/me/coach-notes
POST /v1/admin/matches
POST /v1/admin/matches/:id/confirm
```

服务端根据会话得到内部 `user_id`；不要接受前端传入 `user_id` 来决定私密数据归属。

## 8. 排名与指标

首版推荐可解释的积分制：基础胜场分 + 对手强度加成 + 赛事轮次加成 + 近期衰减。不要一开始就做复杂黑盒算法。

- 排名榜记录赛季、NTRP 分组与更新时间。
- H2H 只统计已确认比赛，可按场地、赛制、时间窗切片。
- 技术指标把“原始数据”和“归一化得分”分开保存。
- 六维雷达必须展示样本数和时间窗，避免少量比赛造成误导。
- 指标无法自动采集时，先由赛后双方/记录员录入少量高价值字段：一发稳定性、双误、制胜分、非受迫失误、网前得分、关键分。

## 9. 建议开发顺序

1. 公开首页、真实球员/比赛导入、赛果确认、积分榜。
2. 微信登录 + Web 登录统一到账户体系，完成会员绑定。
3. 会员数据舱、隐私设置、H2H 细分。
4. 管理后台与教练备注。
5. 视频/高级指标、订阅消息和活动报名。

正式接入真实数据前，优先确认三件事：会员是否同意公开昵称/头像；战力排名采用何种规则；详细指标由谁录入和确认。
